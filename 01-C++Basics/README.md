# 🎯 C++ Basics Milestone

This folder tracks my foundational mastery of C++ syntax, environment mechanics, and control flow before kicking off heavy DSA logic.

## 📓 Problem Index

### 1. Hello World (`hello.cpp`)
* **Problem Statement:** Initialize the development environment, configure native GCC compilers, and output clean string metrics to the terminal.

### 2. Live Metric Tracking (`user_input.cpp`)
* **Problem Statement:** Build an interactive console program that captures user age and target daily DSA focus hours to compute total sprint output dynamically.

### 3. Iterative Schedule Validation (`loop_practice.cpp`)
* **Problem Statement:** Design a continuous execution loop evaluating daily training schedules that runs indefinitely until the user inputs an explicit exit signal (`0`).